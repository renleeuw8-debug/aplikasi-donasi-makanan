# Konfigurasi Server Backend

Rename file ini menjadi `.env` dan sesuaikan dengan konfigurasi Anda

```
PORT=3000

# Database MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=donasi_makanan

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_change_this

# Server URL (untuk development)
SERVER_URL=http://localhost:3000
```
